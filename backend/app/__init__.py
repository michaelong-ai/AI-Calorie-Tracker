# This empty file marks the "app" folder as a Python *package*, which lets us
# import from it with statements like `from app.main import app`.
# Without an __init__.py, older tooling and some import setups can't find
# the modules inside this folder.
