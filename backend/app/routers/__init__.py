# Marks "routers" as a Python package. Each file in here groups the API
# endpoints for one feature (entries, goals, weights, ...) — FastAPI calls
# such a group an APIRouter, and main.py plugs each one into the app.
