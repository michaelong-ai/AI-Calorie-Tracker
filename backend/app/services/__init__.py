# Marks "services" as a package. Services hold business logic (calculations,
# rules) with NO web/database code — routers call them. Keeping the logic
# pure like this is what makes it easy to unit test.
