# Normalize line endings: files are stored with LF in the repo, and git
# converts to your OS's native endings on checkout. Prevents the classic
# "every line changed" diffs when Windows (CRLF) and Linux (LF) machines
# touch the same repo.
* text=auto

# Windows scripts genuinely need CRLF on disk.
*.ps1 text eol=crlf
